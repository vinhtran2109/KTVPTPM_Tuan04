# Base image PHP + Apache
FROM php:8.2-apache

# Copy code nếu muốn (tuỳ chọn, chúng ta sẽ mount)
# COPY . /var/www/html/

# Mở cổng 80
EXPOSE 80

# CMD mặc định đã chạy Apache trong base image, không cần override